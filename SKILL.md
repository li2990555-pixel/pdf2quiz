---
name: pdf2quiz
description: 对话式刷题。用户上传 PDF/Word 题库自动解析，AI 逐题出题、判题、解析，支持自然语言调取任意章节/题型/错题/乱序刷题，结束生成 Word 报告。
metadata:
  origin: original
  author: 伏草
  author-profile: ""
  category: content-creation
  backup-category: learning-tools
  tags: [quiz, pdf, study, exam-prep, chat-quiz, word-report]
  version: 1.0.0
  status: active
  install-targets: [workspace]
---

# pdf2quiz — 对话式AI刷题助手

自动识别上传的 PDF/Word 题库文档 → 对话式刷题 → 用自然语言灵活调取任意题目 → 结束后生成 Word 报告。

---

## 核心交互

每轮：AI 出一题 → 用户回答 → AI 判题/解析 → 下一题（或按用户指令切换到其他题目）

---

## 第一步：自动识别 & 解析题库

用户上传文件（PDF 或 Word .docx）后，自动识别为题库文档，无需用户额外声明。

### 自适应解析引擎（行分类打分算法）

不依赖固定模板，而是对每一行进行多维度分类 + 上下文消歧：

1. **归一化**：全角→半角、零宽字符清理、Unicode NFKC、全角空格→半角
2. **行分类**（每行自动判断类型）：

   | 类型 | 判断依据 |
   |------|---------|
   | 章节行 | `第X章` / `导论` / `Chapter X` / `Part X` 开头 |
   | 题型行 | `一.单项选择题` / `二、多选题` / `三．判断` 等 |
   | 题号行 | `1.` / `①` / `(1)` / `【1】` 开头 |
   | 选项行 | `A.` / `A、` / `A)` 开头，或 `对/错/√/×` 单字 |
   | 答案行 | `答案：` / `参考答案：` / `正确答案：` / `答：` |
   | 解析行 | `解析：` / `详解：` / `讲解：` / `解答：` |
   | 正文行 | 以上都不是（自动合并到最近的题号行作为题干） |

3. **上下文修正**：连续正文行→合并为题干；同一行多个选项（`A.xx B.xx C.xx D.xx`）→自动拆分
4. **题型自动推断**（无显式题型标记时）：
   - 选项 = 2 且内容为对/错 → 判断题
   - 选项 ≥ 4 且答案含多字母 → 多选题
   - 无选项 → 简答题
5. **行内答案**：自动识别 `（B）`、`【A】` 等标记并剥离到 answer 字段

**解析结果展示：**

```
📄 已解析「2022春题库客观题」
共 297 道题，8 个章节：

 1️⃣ 导论（15 题）
 2️⃣ 第一章 毛泽东思想及其历史地位（35 题）
 3️⃣ 第二章 新民主主义革命理论（42 题）
 ...

想怎么刷？可以直接说：
  - "做 20 道"（从第一章开始顺序刷）
  - "先刷第五章 邓小平理论"
  - "先刷所有多选题"
  - "打乱各章节混着刷"
  - "随机抽 30 道"
```

> 如果文件不是题库（无题目特征），告知用户并提供退出选项。

---

## 第二步：自然语言指令系统（核心）

**这是本 skill 的核心能力。用户可以用自然语言指定刷题方式，必须正确理解并执行。**

### 指令分类与处理

#### 1️⃣ 按章节刷题

| 用户说 | 行为 |
|--------|------|
| "先刷第一章" / "从第一章开始" | 筛选该章节题目作为当前刷题集 |
| "跳着刷，先刷第五章" | 切换到第五章的题目 |
| "我想刷导论" | 切换到导论部分 |
| "刷完第一章再刷第二章" | 顺序排列多个章节 |

#### 2️⃣ 按题型刷题

| 用户说 | 行为 |
|--------|------|
| "先刷所有单选题" / "我只想做单选" | 从当前范围内筛选出所有单选题 |
| "先刷所有的填空题" | 筛选填空题 |
| "所有多选题一起刷" | 筛选多选题 |

#### 3️⃣ 混合/乱序刷题

| 用户说 | 行为 |
|--------|------|
| "打乱各个章节来刷" / "混着刷" | 从所有章节随机取题，打乱顺序 |
| "随机抽 30 道" | 从全题库随机抽取指定数量 |
| "把第一章和第三章混在一起刷" | 合并指定章节并打乱 |

#### 4️⃣ 错题刷题

| 用户说 | 行为 |
|--------|------|
| "我想刷一下错题" | 调出错题题库，刷所有错题 |
| "重刷错题" | 同上 |
| "错题再来一遍" | 同上 |

#### 5️⃣ 进度控制

| 用户说 | 行为 |
|--------|------|
| "继续" / "下一题" | 继续当前刷题集下一题 |
| "先到这儿吧" / "停止" | 结束并出报告 |
| "重新开始" | 重置所有进度重新来 |
| "做了多少了" / "进度" | 展示当前进度 |
| "还有多少题没做" | 展示未刷题目数量 |
| "跳过这题" / "不会" | 标记为跳过（算错题），进入下一题 |

### 执行原则

- **自动跟踪未刷题目**：每次筛选刷题集时，自动计算并记录每个章节/各题型中已刷和未刷的题目数量
- **"刷新"而非"替换"**：切换到新内容后，之前的进度仍然保留（已刷的题标记不变），重新切回时继续
- **柔性理解**："我要刷第五章" = "先刷第五章" = "做邓论部分" = "从第五章开始" 都应正确响应
- **刷题集生成后**：展示当前刷题集摘要，然后出第一题

```
📋 当前刷题集：第五章 邓小平理论（51 题）
其中未做：35 题（单选 25 题 + 多选 10 题）

开始：
━━━━━━━━━━━━━━━━━━━━━━━━━━
📌 第 1/35 题 | 单选题 | ✅ 0 | ❌ 0
...
```

---

## 第三步：逐题交互

### 出题格式

```
━━━━━━━━━━━━━━━━━━━━━━━━━━
📌 第 3/35 题 | 单选题 | ✅ 2 | ❌ 0

题干内容...

A. 选项一
B. 选项二
C. 选项三
D. 选项四
━━━━━━━━━━━━━━━━━━━━━━━━━━

输入答案（A/B/C/D），或输入指令（如"跳过"、"停止"）：
```

### 判题与反馈

```
✅ 回答正确！
解析：...

━━━━━━━━━━━━━━━━━━━━━━━━━━
📌 第 4/35 题 | ...
```

```
❌ 回答错误
你的答案：A
正确答案：B
（该题已自动加入错题本）

解析：...

━━━━━━━━━━━━━━━━━━━━━━━━━━
📌 第 4/35 题 | ...
```

**多选题判题**：严格按完整答案字母串匹配，完全一致才算对。

### 题型处理

| 题型 | 用户输入 | 判题方式 |
|------|---------|---------|
| 单选题 | A/B/C/D | 精确匹配 |
| 多选题 | 字母组合如 ABC | 精确匹配完整字符串 |
| 判断题 | 对/错 或 ✓/✗ | 归一化匹配 |
| 填空题 | 文本 | 去空格/大小写后宽松匹配 |
| 简答题 | 自由文本 | 展示参考答案，用户自评 |

---

## 第四步：错题题库（自动维护）

**每答错一题，自动加入错题本。** 无需用户手动操作。

### 错题本数据结构（在对话中维护）

```
错题本 = {
  题目ID: {
    question: "...",
    correctAnswer: "B",
    userAnswer: "A",
    explanation: "...",
    sourceChapter: "第一章"
  }
}
```

### 错题交互

用户说"刷错题" / "错题再来一遍" 时：

```
📕 错题本：共 5 道错题
来源：导论（1 题）、第一章（3 题）、第三章（1 题）

开始重刷错题：
━━━━━━━━━━━━━━━━━━━━━━━━━━
📌 错题 1/5 | 单选题 | ✅ 0 | ❌ 0
...
```

- 错题刷完正确的，可以选择"移出错题本"或保留
- 如果用户再次答错，错题本保留该题
- 重新上传题库后，错题本重置

---

## 第五步：结束与报告

### 结束条件

1. 当前刷题集全部完成
2. 用户说"停止"、"先到这儿吧"、"不做了"
3. 用户说"重新开始"（重置所有）

### 生成 Word 报告

在终止条件触发后，调用 python-docx 生成 Word 文档（.docx），写入桌面。

**报告内容：**

```
📊 答题报告

题库：毛概2022春
刷题范围：第五章 邓小平理论（已完成 35/51 题）

成绩统计：
  正确：28 题
  错误：7 题
  正确率：80%

✅ 已答题目：
  第 1 题（单选题）你的答案：B ✓
  第 2 题（单选题）你的答案：C ✓
  ...

📕 错题回顾：
  第 3 题（单选题）你的答案：A → 正确答案：B
  第 8 题（多选题）你的答案：AB → 正确答案：ABC
  ...

⏳ 未刷题目：
  还有 16 题未做（选择题 12 题 + 多选题 4 题）
  下次可以从第 36 题继续，或使用指令切换到其他章节
```

---

## 对话状态维护

整个对话中维护以下状态（不要重置，除非用户说"重新开始"）：

```
state = {
  // 原始题库（全量）
  rawBanks: { "章节名": [题目数组] },
  
  // 当前刷题集（筛选后的题目列表）
  currentSet: [题目ID数组],
  
  // 当前刷题索引
  currentIndex: 0,
  
  // 答案记录 { 题目ID: 用户答案 }
  answers: {},
  
  // 错题本 { 题目ID: { 详情 } }
  wrongBook: {},
  
  // 各章节进度 { "章节名": { total, answered } }
  progress: {},
  
  // 刷题集名称（用于报告）
  setName: "第五章 邓小平理论"
}
```

---

## 自然语言理解指南

当用户说一句话时，按以下优先级判断意图：

1. **错题指令**：包含"错题" → 调出错题本
2. **章节指令**：包含"章"、"节"、章节名称关键词（导论/第一章/毛泽东思想等）→ 切换到对应章节
3. **题型指令**：包含"单选"、"多选"、"判断"、"填空"、"简答" → 筛选题型
4. **乱序指令**：包含"打乱"、"混"、"随机" → 打乱合并
5. **数量指令**：包含数字 + "道"、"题" → 随机抽取指定数量
6. **进度指令**：包含"进度"、"多少"、"还有" → 展示进度摘要
7. **停止指令**：包含"停"、"到这"、"不做了" → 结束出报告
8. **跳过指令**：包含"跳过"、"不会" → 标记跳过，下一题
9. **继续指令**：输入单个字母（A/B/C/D等）→ 视为答题
10. **默认**：输入任意字母 → 视为选择题答案

> 重要：不要用僵硬的关键词匹配，而是理解用户的真实意图。例如"我想看看邓论部分"应识别为"第五章"，"做做单选"应识别为"筛选单选题"。

---

## 前置依赖

使用本 Skill 前需要准备：

- **运行环境**：支持 PDF/Word 解析的 Agent 环境（Codex / Claude / 同类）
- **Python 依赖**（生成 Word 报告时需要）：
  - `python-docx`（pip install python-docx）
  - `pdfplumber` 或 `PyPDF2`（解析 PDF 题库）
  - `python-docx`（解析 Word 题库，可选）
- **题库文件**：用户自备 PDF 或 Word 格式的题库文档

### Preflight Checklist

- [ ] Python ≥ 3.9 已安装
- [ ] `python-docx` 已安装（`pip show python-docx` 能找到）
- [ ] PDF 解析库已安装（`pdfplumber` 推荐）
- [ ] 准备好要刷的题库文件（PDF/Word）

未完成以上准备时，Skill 会**提示用户先完成**，不会自动代办。

---

## 适用场景

✅ **适合：**
- 考研 / 法考 / 医考 / 教师资格等考试备考
- 用户已有 PDF/Word 题库文件
- 想要按章节、题型、错题等自然语言指令灵活刷题
- 刷题过程中希望 AI 给出详细解析

❌ **不适合：**
- 用户只有图片扫描版 PDF（OCR 失败率高）
- 用户希望直接刷教材目录而非题库
- 用户没有题库文件，需要先整理题库

---

## 输入与输出

**输入：** PDF 或 Word 格式的题库文件 + 自然语言刷题指令

**输出：**
- 解析后的题库结构（章节、题型、题号）
- 逐题对话（出题、判题、解析）
- 错题本（自动维护）
- 刷题结束后的 Word 报告（.docx）

---

## 原创归属

本 Skill 由 **伏草** 原创。欢迎关注公众号「伏草记」，获取更多 AI、Agent、Skill 与写作工作流分享。
